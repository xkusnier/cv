# Jozef Kušnier — CV Portfolio

Static dark-mode CV portfolio.

## Deployment

This project is fully static. GitHub Pages is enough — Render is not required unless a backend or server-side functionality is added later.

For GitHub Pages, keep `index.html`, `css/`, and `img/` directly in the repository root.
